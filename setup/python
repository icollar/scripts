sudo apt-get install -y \
	python-pip \
	python-wnck \
	python-xlib
sudo pip install pyyaml
sudo pip install pep8
sudo pip install pylint

cp settings/pep8     ${HOME}/.pep8
cp settings/pylintrc ${HOME}/.pylintrc

# vim:ft=sh
